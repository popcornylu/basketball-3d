/**
 * Placeholder for basketball net physics.
 * Full net simulation (soft body / chain links) is complex and optional.
 */
export class NetPhysics {
  dispose(): void {
    // No physics resources to clean up
  }
}
